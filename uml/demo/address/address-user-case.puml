@startuml
left to right direction

rectangle 基础数据 {
    (新增地址)
    (修改地址名称)
    (删除地址)
    (查询子地址列表)
    (查询地址信息)
    (校验地址级联关系是否正确)
    (记录自定义地址)
    (展示自定义地址列表)
    (自定义地址审核通过)
    (自定义地址审核不通过)
    (导出地址数据为文件供前端使用)
    (导出印度pincode数据为文件供前端使用)
}

rectangle 库存中心 {
    (新增不配送地址)
    (查询不配送地址)
    (删除不配送地址)
    (判断地址是否是不配送地址)
}


商城 --> (查询子地址列表)
商城 --> (查询地址信息)
商城 --> (校验地址级联关系是否正确)
商城 --> (记录自定义地址)
管理员 --> (新增地址)
管理员 --> (修改地址名称)
管理员 --> (删除地址)
管理员 --> (展示自定义地址列表)
管理员 --> (自定义地址审核通过)
管理员 --> (自定义地址审核不通过)
管理员 --> (新增不配送地址)
管理员 --> (查询不配送地址)
管理员 --> (删除不配送地址)
管理员 --> (导出地址数据为文件供前端使用)
管理员 --> (导出印度pincode数据为文件供前端使用)
商城 --> (判断地址是否是不配送地址)
@enduml